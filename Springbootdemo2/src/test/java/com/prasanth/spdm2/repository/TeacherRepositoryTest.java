package com.prasanth.spdm2.repository;

import static org.junit.jupiter.api.Assertions.*;

import java.util.ArrayList;
import java.util.List;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

import com.prasanth.spdm2.entity.Course;
import com.prasanth.spdm2.entity.Teacher;

@SpringBootTest
class TeacherRepositoryTest {

	/*
	 * @Test void test() { fail("Not yet implemented"); }
	 */
	@Autowired
	private TeacherRepository teacherRepo;
	
	/*
	 * @Test public void saveTeacher() { Course cs=Course.builder() .title("kamui")
	 * .credit(5) .build(); Course cs1=Course.builder() .title("chidori") .credit(8)
	 * .build(); Course cs2=Course.builder() .title("Copytechnic") .credit(10)
	 * .build();
	 * 
	 * Teacher teach=Teacher.builder() .firstName("Kakashi") .lastName("Hatake")
	 * .courses(List.of(cs,cs1,cs2)) .build(); teacherRepo.save(teach);
	 * 
	 * }
	 */
	}
